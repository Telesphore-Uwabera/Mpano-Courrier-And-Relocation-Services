window.initMap = async function() {
    let o = window.innerWidth
      , n = 3.82;
    o < 1440 && (n = 3.4),
    o < 900 && (n = 2.9),
    o < 600 && (n = 2.8);
    const {Map: e} = await google.maps.importLibrary("maps")
      , {AdvancedMarkerElement: t, PinElement: a} = await google.maps.importLibrary("marker")
      , i = {
        background: "#3b892c",
        borderColor: "#ffffff",
        glyphColor: "#ffffff"
    }
      , r = window.mapCoord ? window.mapCoord : {
        lat: 1,
        lng: 17.2812
    }
      , l = new e(document.getElementById("map"),{
        zoom: n,
        center: r,
        disableDefaultUI: !0,
        zoomControl: !0,
        fullscreenControl: !1,
        mapId: "1ee7815b144d52df"
    });
    window.mapMarkers.forEach((o => {
        const n = new a(i)
          , e = new t({
            map: l,
            position: o.position,
            title: o.title,
            content: n.element
        });
        o.url && e.addListener("click", (function({domEvent: n, latLng: e}) {
            window.location.href = o.url
        }
        ))
    }
    ))
}
;
//# sourceMappingURL=map.min.js.map
